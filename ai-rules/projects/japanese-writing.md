# Japanese Writing Preferences

- When generating Japanese text for documentation, comments, or chat responses, use commas (`,`) and periods (`.`) instead of `、` and `。`.
- Insert a half-width space between Japanese text and numbers, symbols, or English alphabets.
