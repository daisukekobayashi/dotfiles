interface:
  display_name: "Code Review"
  short_description: "Review repository changes for material defects"
  default_prompt: "Use $code-review to review this repository change before shipping."

policy:
  allow_implicit_invocation: false
