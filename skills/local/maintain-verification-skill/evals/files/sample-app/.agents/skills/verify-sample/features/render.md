# Render text

Run `python3 app.py render hello` from the project root.
Expect stdout to be exactly `HELLO` followed by a newline, and exit status 0.
Save the command, exit status, and output in `.work/evidence/`.
